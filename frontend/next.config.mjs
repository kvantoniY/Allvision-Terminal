/** @type {import('next').NextConfig} */
const nextConfig = {
  // If you later decide to store JWT in HttpOnly cookies and authenticate via cookies,
  // enable credentials in fetch and make sure your backend supports cookie auth.
};

export default nextConfig;
